{
  "name": "peachymon",
  "private": true,
  "version": "0.2.0",
  "type": "module",
  "homepage": "https://github.com/LeSteak11/PeachyMon",
  "scripts": {
    "_comment_env": "Scripts that need POKEMMO_MAPS_DIR or R2 creds use Node's built-in --env-file-if-exists flag (Node >= 20.6). '.env.local' is loaded if present, otherwise the existing process env wins. Vite's dev/build don't need this because Vite has its own .env loader.",

    "build:data": "node scripts/build-data.mjs",

    "_comment_trainers": "Build public/data/trainers.json (Gym & E4 Prep) from the gym-leader xlsx parse + wiki E4 scrape. Runs after build:data since it resolves names against public/data/pokemmo.json. Refresh sources: scripts/parse-gym-xlsx.mjs + scripts/scrape-trainers.mjs.",
    "build:trainers": "node scripts/build-trainers.mjs",

    "_comment_merge_trainers": "Merge a Trainer Scribe export into a region's trainerInstances catalog. e.g. npm run merge:trainers -- ~/Downloads/trainer-teams.json --region=johto --dry-run",
    "merge:trainers": "node scripts/merge-trainer-teams.mjs",


    "build:map-data":        "node --env-file-if-exists=.env.local scripts/build-map-data.mjs sinnoh",
    "build:map-data:sinnoh": "node --env-file-if-exists=.env.local scripts/build-map-data.mjs sinnoh",
    "build:map-data:johto":  "node --env-file-if-exists=.env.local scripts/build-map-data.mjs johto",

    "upload:maps":             "node --env-file-if-exists=.env.local scripts/upload-maps-to-r2.mjs sinnoh",
    "upload:maps:sinnoh":      "node --env-file-if-exists=.env.local scripts/upload-maps-to-r2.mjs sinnoh",
    "upload:maps:johto":       "node --env-file-if-exists=.env.local scripts/upload-maps-to-r2.mjs johto",
    "upload:maps:dry":         "node --env-file-if-exists=.env.local scripts/upload-maps-to-r2.mjs sinnoh --dry-run",
    "upload:maps:dry:sinnoh":  "node --env-file-if-exists=.env.local scripts/upload-maps-to-r2.mjs sinnoh --dry-run",
    "upload:maps:dry:johto":   "node --env-file-if-exists=.env.local scripts/upload-maps-to-r2.mjs johto --dry-run",
    "upload:maps:sync":        "node --env-file-if-exists=.env.local scripts/upload-maps-to-r2.mjs sinnoh --prune-stale",
    "upload:maps:sync:sinnoh": "node --env-file-if-exists=.env.local scripts/upload-maps-to-r2.mjs sinnoh --prune-stale",
    "upload:maps:sync:johto":  "node --env-file-if-exists=.env.local scripts/upload-maps-to-r2.mjs johto --prune-stale",

    "regen-and-sync":        "npm run build:map-data:sinnoh && npm run upload:maps:sync:sinnoh",
    "regen-and-sync:sinnoh": "npm run build:map-data:sinnoh && npm run upload:maps:sync:sinnoh",
    "regen-and-sync:johto":  "npm run build:map-data:johto && npm run upload:maps:sync:johto",

    "purge:r2": "node --env-file-if-exists=.env.local scripts/upload-maps-to-r2.mjs --purge",

    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "_comment_predeploy": "After vite build, prune dist/data/maps/<region>/{images,tiles}/ — both are hosted on R2 (absolute URLs in maps-index.json), so they're dead weight in the gh-pages branch.",
    "predeploy": "npm run build:data && npm run build:trainers && npm run build && node scripts/prune-dist-for-gh-pages.mjs",
    "_comment_deploy": "Custom deploy script instead of the gh-pages package. gh-pages' internal Git.rm spreads every tracked file as a separate argv to `git rm`, which on Windows blows past CreateProcess's ~32 KB command-line cap once the branch holds more than a few thousand files (we have ~600 zone WebPs + ~500 event manifests + ~500 walkable sidecars + ~5000 overworld tiles per region). Our script uses `git worktree` + a single `git rm -rf .` cleanup so the argv stays tiny regardless of file count.",
    "deploy": "node scripts/deploy-gh-pages.mjs",

    "_comment_desktop": "Windows desktop shell (Tauri v2). Requires a local Rust toolchain (rustup) + WebView2 — see DESKTOP.md. The same React build (dist/) the website ships is bundled into the desktop app; capture/OCR features are gated on window.__TAURI__ so the website is unaffected.",
    "tauri": "tauri",
    "desktop:dev": "tauri dev",
    "desktop:build": "tauri build",
    "desktop:icon": "tauri icon"
  },
  "dependencies": {
    "leaflet": "^1.9.4",
    "lucide-react": "^0.383.0",
    "pokemmo-calc": "file:vendor/pokemmo-calc",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-leaflet": "^4.2.1",
    "react-router-dom": "^7.14.2"
  },
  "devDependencies": {
    "@aws-sdk/client-s3": "^3.700.0",
    "@tauri-apps/cli": "^2.0.0",
    "@vitejs/plugin-react": "^4.3.1",
    "sharp": "^0.34.0",
    "autoprefixer": "^10.4.20",
    "gh-pages": "^6.1.1",
    "postcss": "^8.4.41",
    "tailwindcss": "^3.4.10",
    "vite": "^5.4.2"
  }
}
